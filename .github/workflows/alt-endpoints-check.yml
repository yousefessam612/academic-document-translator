name: Free endpoints reachability check

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Probe free LLM endpoints from datacenter IP
        run: python scripts/free_endpoints_probe.py
